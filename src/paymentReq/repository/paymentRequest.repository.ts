import { Repository } from "typeorm";
import { PaymentRequest } from "../entity/paymentRequest.entity";

export class PaymentRequestRepository extends Repository<PaymentRequest> {
    // Add custom methods for the Address entity if needed
  }
  
